name: Bootstrap USB System (disabled automatic sync)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: USB repository independence notice
        run: |
          echo "Automatic copying from the web repository has been disabled."
          echo "The USB edition is maintained independently."
